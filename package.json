{
  "name": "star-paper",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "preview": "npx --yes serve . -l 8080",
    "preview:alt": "npx --yes serve . -l 8081",
    "preflight": "node scripts/preflight.mjs",
    "build": "npm run preflight",
    "test": "npm run preflight && node --test mock-removal.test.mjs auth-retry-queue.test.mjs security-invariants.test.mjs",
    "prepublishOnly": "npm run preflight"
  }
}
