{
  "name": "star-paper",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "preflight": "node scripts/preflight.mjs",
    "build": "npm run preflight",
    "test": "npm run preflight && node --test mock-removal.test.mjs auth-retry-queue.test.mjs",
    "prepublishOnly": "npm run preflight"
  }
}
